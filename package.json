{
  "name": "player-helper",
  "version": "2.7.3",
  "description": "Browser extension to automate video player interactions (speed, ads, view mode)",
  "private": true,
  "scripts": {
    "lint": "eslint src/ popup/js/",
    "lint:fix": "eslint src/ popup/js/ --fix",
    "build:chrome": "cp manifest/manifest.chrome.json manifest.json",
    "build:firefox": "cp manifest/manifest.firefox.json manifest.json",
    "build:web-ext": "web-ext build"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "web-ext": "^8.0.0"
  }
}
